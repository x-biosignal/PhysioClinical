% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cohort-scoring.R
\name{scoreCohort}
\alias{scoreCohort}
\title{Score a cohort's instrument responses into interop-ready ClinicalScores}
\usage{
scoreCohort(
  responses,
  instrument,
  subject_col = "subject_id",
  timestamp_col = NULL,
  missing = c("error", "prorate", "na")
)
}
\arguments{
\item{responses}{A data.frame: one row per subject, with a subject-id column
and one column per instrument item (columns not matching an item are
ignored).}

\item{instrument}{A \link{ClinicalInstrument} or an instrument id (resolved via
\code{\link[=getInstrument]{getInstrument()}}).}

\item{subject_col}{Name of the subject-id column (default \code{"subject_id"}).}

\item{timestamp_col}{Optional name of a timestamp column stored on each score.}

\item{missing}{Missing-data policy passed to \code{\link[=scoreInstrument]{scoreInstrument()}} (default
\code{"error"}; use \code{"prorate"} / \code{"na"} when only some items are present).}
}
\value{
A named list (by subject id) of \link{ClinicalScore} objects.
}
\description{
Scores a per-subject table of instrument responses into a list of
\link{ClinicalScore} objects, each tagged with its \code{subject_id}, ready to drive the
FHIR / OMOP / CDISC exporters (\code{\link[=writeFHIRBundle]{writeFHIRBundle()}}, \code{\link[=toOMOP]{toOMOP()}},
\code{\link[=toCDISC_QS]{toCDISC_QS()}}) directly. The \code{subject_col} and item columns can come straight
from a cohort's data (e.g. a \code{PhysioCohort} subject table joined to captured
responses).
}
\examples{
items <- getInstrument("katz_adl")@items
resp <- data.frame(subject_id = c("P01", "P02"),
  stats::setNames(as.data.frame(rbind(rep(1, 6), c(1, 0, 1, 1, 0, 1))), items))
scores <- scoreCohort(resp, "katz_adl")
vapply(scores, function(s) s@total, numeric(1))
}
\seealso{
\code{\link[=scoreInstrument]{scoreInstrument()}}, \code{\link[=writeFHIRBundle]{writeFHIRBundle()}}, \code{\link[=toOMOP]{toOMOP()}}, \code{\link[=toCDISC_QS]{toCDISC_QS()}}
}
