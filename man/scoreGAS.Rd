% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gas.R
\name{scoreGAS}
\alias{scoreGAS}
\title{Score Goal Attainment Scaling (GAS T-score)}
\usage{
scoreGAS(goals, attained_levels, rho = 0.3)
}
\arguments{
\item{goals}{A \code{\link{defineGoal}} object or a list of them.}

\item{attained_levels}{Numeric attained level for each goal (each within that
goal's \code{levels}).}

\item{rho}{Assumed inter-goal correlation in \code{[0, 1)}; default 0.3.}
}
\value{
A \code{"gas_result"} with the T-score, per-goal weights/levels and
\code{rho}.
}
\description{
Computes the standardized GAS T-score across a set of goals given their
attained levels:
\deqn{T = 50 + \frac{10 \sum_i w_i x_i}{\sqrt{(1-\rho)\sum_i w_i^2 +
  \rho (\sum_i w_i)^2}}}
where \eqn{x_i} is the attained level, \eqn{w_i} the weight and \eqn{\rho}
the assumed inter-goal correlation. All goals at their expected level
(\eqn{x_i = 0}) give \eqn{T = 50}.
}
\examples{
g1 <- defineGoal("Transfers", importance = 3, difficulty = 2)
g2 <- defineGoal("Stairs", importance = 2, difficulty = 3)
scoreGAS(list(g1, g2), attained_levels = c(1, 0))
}
\references{
Kiresuk & Sherman (1968); Turner-Stokes (2009).
}
\seealso{
\code{\link[=defineGoal]{defineGoal()}}, \code{\link[=gasSummary]{gasSummary()}}
}
