% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interop-cdisc.R
\name{toCDISC_QS}
\alias{toCDISC_QS}
\title{Export clinical scores to a CDISC SDTM QS (Questionnaires) domain}
\usage{
toCDISC_QS(
  scores,
  ct_map = NULL,
  studyid = "STUDY",
  usubjid_map = NULL,
  visitnum = 1L,
  visit = NA_character_
)
}
\arguments{
\item{scores}{A \code{\linkS4class{ClinicalScore}} or a list of them.}

\item{ct_map}{Optional data frame overriding the packaged CT map (columns
\code{instrument}, \code{subscale}, \code{qstestcd}, \code{qstest},
\code{qscat}).}

\item{studyid}{\code{STUDYID} value (default \code{"STUDY"}).}

\item{usubjid_map}{Optional named vector/list mapping \code{subject_id} to a
\code{USUBJID}; otherwise \code{USUBJID = <studyid>-<subject_id>}.}

\item{visitnum, visit}{\code{VISITNUM} / \code{VISIT} for all records.}
}
\value{
A QS-domain \code{data.frame} conforming to the SDTMIG column layout.
}
\description{
Maps one or more \code{\linkS4class{ClinicalScore}} objects to a CDISC SDTMIG
Questionnaires (QS) domain data frame: the total and each subscale becomes a
QS record. \code{QSTESTCD}/\code{QSTEST}/\code{QSCAT} come from the packaged
controlled-terminology map (\code{cdisc_ct.csv}); an unmapped instrument gets
a sponsor-defined \code{QSTESTCD} (upper-cased, forced to start with a letter,
disambiguated against collisions, and truncated to the SDTM 8-character limit)
with a warning. \code{QSTESTCD} values in the shipped map are sponsor-defined
(\code{ct_status = "sponsor"}), not asserted to be published CDISC CT.
}
\examples{
sc <- methods::new("ClinicalScore", instrument_id = "fim", total = 90,
                   subscales = c(motor = 60, cognitive = 30),
                   subject_id = "01-001", timestamp = "2026-07-26")
toCDISC_QS(sc)
}
\references{
CDISC SDTMIG QS domain.
}
\seealso{
\code{\link[=toADaM_ADQS]{toADaM_ADQS()}}, \code{\link[=toFHIRObservation]{toFHIRObservation()}}, \code{\link[=toOMOP]{toOMOP()}}
}
