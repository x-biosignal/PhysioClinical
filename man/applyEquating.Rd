% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crosswalk.R
\name{applyEquating}
\alias{applyEquating}
\title{Apply a score crosswalk to new values}
\usage{
applyEquating(equating, values)
}
\arguments{
\item{equating}{a \code{score_equating} from \code{\link[=equateScores]{equateScores()}}.}

\item{values}{numeric source-scale values to convert.}
}
\value{
numeric target-scale equivalents (linearly interpolated within the
crosswalk, clamped to its range).
}
\description{
Apply a score crosswalk to new values
}
\examples{
set.seed(1)
ability <- rnorm(200)
barthel <- pmin(100, pmax(0, round((ability + 3) * 16 / 5) * 5))
fim <- pmin(126, pmax(18, round(18 + (ability + 3) * 18)))
applyEquating(equateScores(barthel, fim), c(40, 60, 80))
}
\seealso{
\code{\link[=equateScores]{equateScores()}}
}
