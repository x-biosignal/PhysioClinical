% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/com-score.R
\name{scoreInstrument}
\alias{scoreInstrument}
\title{Score item responses against a clinical instrument}
\usage{
scoreInstrument(
  instrument,
  items,
  missing = c("error", "prorate", "na"),
  subject_id = NA_character_,
  timestamp = NA_character_
)
}
\arguments{
\item{instrument}{A \link{ClinicalInstrument}, or an instrument id resolved via
\code{\link[=getInstrument]{getInstrument()}}.}

\item{items}{A named numeric vector (item id -> response), a one-row
data.frame, or an unnamed vector in the instrument's item order.}

\item{missing}{Missing-data policy: \code{"error"} (default; any missing item is
an error), \code{"prorate"} (scale the observed items up to the full count) or
\code{"na"} (return \code{NA} for any score with a missing item).}

\item{subject_id}{Optional subject identifier stored on the result.}

\item{timestamp}{Optional timestamp stored on the result.}
}
\value{
A \link{ClinicalScore}.
}
\description{
Validates a set of item responses against a \link{ClinicalInstrument}, aggregates
them into the total and subscale scores under the chosen missing-data policy,
and assigns the interpretation stratum.
}
\examples{
inst <- ClinicalInstrument(
  id = "toy", items = c("q1", "q2", "q3"),
  item_ranges = list(q1 = c(0, 4), q2 = c(0, 4), q3 = c(0, 4)))
scoreInstrument(inst, c(q1 = 2, q2 = 3, q3 = 4))
}
\seealso{
\link{ClinicalInstrument}, \code{\link[=getInstrument]{getInstrument()}}
}
