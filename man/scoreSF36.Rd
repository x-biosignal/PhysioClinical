% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/com-instruments-hrqol.R
\name{scoreSF36}
\alias{scoreSF36}
\title{Score the SF-36 / RAND-36 Health Survey}
\usage{
scoreSF36(items, ...)
}
\arguments{
\item{items}{Named numeric raw responses on the questionnaire's own answer
scales (\code{q01}, \code{q03}..\code{q36}, omitting the health-transition item \code{q02}), or
an unnamed vector in the instrument's item order.}

\item{...}{Passed to \code{\link[=scoreInstrument]{scoreInstrument()}} (e.g. \code{missing = "prorate"} to score
a scale from its completed items).}
}
\value{
A \link{ClinicalScore} whose \code{subscales} hold the eight 0-100 scale scores
(the \code{total} is the grand mean over all items, not a standard SF-36 score).
}
\description{
Scores the RAND 36-Item Health Survey 1.0: each item's raw response is recoded
to 0-100 (via the instrument's \code{item_recode} map) and averaged within its
scale, giving the eight subscale scores (0-100, higher = better health) in the
returned \code{subscales}: physical functioning, role-physical, role-emotional,
energy/fatigue, emotional well-being, social functioning, pain and general
health. The health-transition item is not part of the eight scales. The
norm-based T-scores and the PCS/MCS component summaries use proprietary
population weights and are out of scope.
}
\examples{
# all best-health responses -> every subscale scores 100
best <- stats::setNames(
  c(1, rep(3, 10), rep(2, 7), 1, 1, 1, 1, 6, 6, 1, 1, 6, 6, 1, 6, 5, 5, 1, 5, 1),
  getInstrument("sf36")@items)
scoreSF36(best)@subscales
}
\references{
Hays RD, Sherbourne CD, Mazel RM (1993). The RAND 36-Item Health
Survey 1.0. \emph{Health Econ} 2:217-227.
}
\seealso{
\code{\link[=scoreEQ5D5L]{scoreEQ5D5L()}}, \code{\link[=getInstrument]{getInstrument()}}
}
