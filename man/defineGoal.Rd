% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gas.R
\name{defineGoal}
\alias{defineGoal}
\title{Define a Goal Attainment Scaling goal}
\usage{
defineGoal(
  description,
  levels = -2:2,
  weight = 1,
  importance = NULL,
  difficulty = NULL,
  icf_tag = NULL
)
}
\arguments{
\item{description}{Free-text goal description.}

\item{levels}{Ordered numeric attainment levels (must include 0, the expected
outcome); default \code{-2:2}.}

\item{weight}{Goal weight when \code{importance}/\code{difficulty} are not
supplied (default 1, positive).}

\item{importance, difficulty}{Optional 0+ ratings; if both given the weight is
\code{importance * difficulty}.}

\item{icf_tag}{Optional ICF code (e.g. \code{"d450"}) linking the goal to the
International Classification of Functioning.}
}
\value{
A \code{"gas_goal"} object.
}
\description{
Describes one GAS goal: its attainment levels (typically \code{-2:2} with 0 =
expected outcome), a weight, and an optional ICF linkage. When both
\code{importance} and \code{difficulty} are given the (Turner-Stokes) weight
is their product; otherwise \code{weight} is used (default 1, i.e. unweighted
GAS).
}
\examples{
defineGoal("Walk 100 m unaided", importance = 3, difficulty = 2,
           icf_tag = "d450")
}
\references{
Kiresuk & Sherman (1968); Turner-Stokes (2009).
}
\seealso{
\code{\link[=scoreGAS]{scoreGAS()}}
}
