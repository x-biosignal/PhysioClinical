% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crosswalk.R
\name{equateScores}
\alias{equateScores}
\title{Equate one clinical score scale to another (build a crosswalk)}
\usage{
equateScores(
  from,
  to,
  method = c("equipercentile", "linear"),
  from_scores = sort(unique(from[is.finite(from)]))
)
}
\arguments{
\item{from}{Numeric source-scale scores from the linking sample.}

\item{to}{Numeric target-scale scores from the linking sample (equipercentile
uses the two marginal distributions; linear uses their means and SDs).}

\item{method}{\code{"equipercentile"} (default) or \code{"linear"}.}

\item{from_scores}{Source scores to tabulate (default: the sorted unique
observed source scores).}
}
\value{
an object of class \code{score_equating}: a list with \code{table}
(data.frame \code{from}, \code{to}), \code{method}, and the linking-sample size. Convert
new values with \code{\link[=applyEquating]{applyEquating()}}.
}
\description{
Constructs a conversion from a "source" scale to a "target" scale using a
linking sample measured on both. Equipercentile equating (the default) maps a
source score to the target score with the same percentile rank, so it handles
the non-linear, bounded relationship typical of ADL scales; linear equating
matches only the mean and SD.
}
\examples{
set.seed(1)
ability <- rnorm(200)
barthel <- pmin(100, pmax(0, round((ability + 3) * 16 / 5) * 5))
fim <- pmin(126, pmax(18, round(18 + (ability + 3) * 18)))
eq <- equateScores(barthel, fim)          # Barthel -> FIM crosswalk
head(eq$table)
}
\references{
Kolen MJ, Brennan RL (2004) Test Equating, Scaling, and Linking;
Houlden H et al. (2006) Clin Rehabil 20:153-159 (Barthel<->FIM).
}
\seealso{
\code{\link[=applyEquating]{applyEquating()}}
}
