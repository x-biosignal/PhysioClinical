% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/com-promis.R
\name{promisRawToT}
\alias{promisRawToT}
\title{Convert a PROMIS summed score to a T-score via a lookup table}
\usage{
promisRawToT(raw, conversion_table)
}
\arguments{
\item{raw}{Summed raw score.}

\item{conversion_table}{A data.frame with a \code{raw} column, a \code{tscore} column
and optionally an \code{se} column (the published conversion table).}
}
\value{
A list with \code{tscore} and \code{se} (\code{NA} if the table has no \code{se} column);
\code{NA} with a warning if \code{raw} is not in the table.
}
\description{
The other official PROMIS scoring path for a full short form: map the summed
raw score to a T-score with the instrument's published summed-score-to-T-score
table. The table is not bundled - obtain it from the HealthMeasures scoring
manual for the specific short form.
}
\examples{
tab <- data.frame(raw = 4:8, tscore = c(21.5, 30.1, 35.7, 40.2, 44.0))
promisRawToT(6, tab)
}
\seealso{
\code{\link[=scorePROMIS]{scorePROMIS()}}
}
