% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/com-performance-tests.R
\name{score10MWT}
\alias{score10MWT}
\title{Score a 10-metre walk test (10mWT)}
\usage{
score10MWT(
  x = NULL,
  time_s = NULL,
  distance = 10,
  pace = c("comfortable", "fast"),
  ref = NULL,
  covariates = list()
)
}
\arguments{
\item{x}{A gait speed (m/s), a \code{walk_test_report}, or a
\code{gait_parameters}; or \code{NULL} to use \code{time_s}.}

\item{time_s}{Walk time in seconds (manual path; speed = \code{distance/time}).}

\item{distance}{Walk distance in metres (default 10).}

\item{pace}{\code{"comfortable"} (default) or \code{"fast"}.}

\item{ref}{Optional \code{\linkS4class{GovernedNormativeReference}} for the
normative z-score.}

\item{covariates}{Covariates (e.g. \code{list(age, sex)}) for \code{ref}.}
}
\value{
A \code{"performance_test_score"} with \code{gait_speed},
\code{ambulation} and (if \code{ref}) \code{zscore}.
}
\description{
Computes comfortable/fast gait speed and its ambulation category, with an
optional normative z-score. Accepts a gait speed directly, a walk time, or a
PhysioMoCap signal-derived result (\code{walk_test_report} from
\code{PhysioMoCap::instrumented10mWT} or \code{gait_parameters} from
\code{PhysioMoCap::calculateGaitParameters}).
}
\examples{
score10MWT(time_s = 8)         # 10 m / 8 s = 1.25 m/s -> community ambulator
}
\references{
Perry et al. (1995); Fritz & Lusardi (2009); Perera et al. (2006).
}
\seealso{
\code{\link[=score6MWT]{score6MWT()}}, \code{\link[=scoreTUG]{scoreTUG()}}
}
