% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/com-rasch-bridge.R
\name{raschAnalyze}
\alias{raschAnalyze}
\title{Rasch (interval) analysis of a clinical ADL/IADL instrument}
\usage{
raschAnalyze(instrument, responses, model = c("PCM", "RSM"), ...)
}
\arguments{
\item{instrument}{A \link{ClinicalInstrument} or an instrument id (e.g.
\code{"barthel"}, \code{"lawton_iadl"}).}

\item{responses}{A persons x items matrix or data.frame of responses on the
instrument's native scale (see \code{\link[=raschRecode]{raschRecode()}}).}

\item{model}{\code{"PCM"} (default) or \code{"RSM"}.}

\item{...}{Passed to \code{\link[PhysioAppKit:pcm_measure]{PhysioAppKit::pcm_measure()}}.}
}
\value{
The \code{poly_rasch} fit (see \code{\link[PhysioAppKit:pcm_measure]{PhysioAppKit::pcm_measure()}}) with an added
class \code{clin_rasch} and extra fields: \code{instrument} (id), \code{item_hierarchy}
(items ordered hardest-to-easiest by calibrated location) and \code{recoded}
(the category matrix analysed).
}
\description{
Fits a polytomous Rasch model to a cohort's responses on a registered
instrument and returns the psychometric summary that published ADL/IADL
analyses report: the item difficulty hierarchy, category threshold ordering
(with disordered-threshold flags), person and item measures on an interval
(logit) scale, item fit (infit/outfit), person/item separation reliability,
and the raw-score to interval-measure conversion. The Partial Credit Model is
the default (it accommodates the Barthel Index's mixed item lengths); pass
\code{model = "RSM"} for a uniform-format scale.
}
\details{
Requires the suggested package \pkg{PhysioAppKit} (the domain-neutral engine).
}
\examples{
\donttest{
set.seed(1)
items <- getInstrument("barthel")@items
# a small synthetic cohort of weighted Barthel responses
resp <- t(replicate(50, {
  ability <- stats::rnorm(1)
  vapply(c(2, 1, 1, 2, 2, 2, 2, 3, 3, 2), function(m)
    c(0, 5, 10, 15)[min(m, max(0, round(ability + m / 2))) + 1], numeric(1))
}))
colnames(resp) <- items
if (requireNamespace("PhysioAppKit", quietly = TRUE)) {
  fit <- raschAnalyze("barthel", resp)
  fit$item_hierarchy
}
}
}
\seealso{
\code{\link[=raschRecode]{raschRecode()}}, \code{\link[=scoreBarthel]{scoreBarthel()}}, \code{\link[PhysioAppKit:pcm_measure]{PhysioAppKit::pcm_measure()}}
}
