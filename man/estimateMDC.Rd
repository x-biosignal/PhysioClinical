% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clinimetrics-estimate.R
\name{estimateMDC}
\alias{estimateMDC}
\title{Distribution-based Minimal Detectable Change from test-retest data}
\usage{
estimateMDC(
  test_retest,
  method = "distribution",
  confidence = 0.95,
  model = c("twoway", "oneway")
)
}
\arguments{
\item{test_retest}{A subjects-by-occasions numeric matrix (or data.frame) of
repeated measurements.}

\item{method}{Estimation method; currently \code{"distribution"}.}

\item{confidence}{Confidence level for the MDC (default 0.95).}

\item{model}{ICC model passed to \code{PhysioCore::icc} (default
\code{"twoway"}).}
}
\value{
A list with \code{mdc}, \code{sem}, \code{icc} and \code{confidence}.
}
\description{
Estimates the MDC from a test-retest reliability study: the intraclass
correlation gives the reliability, the standard error of measurement is
\eqn{SD\sqrt{1 - ICC}}, and \eqn{MDC = SEM \times z \times \sqrt{2}}
(delegating to \code{PhysioCore::icc}/\code{sem}/\code{mdc}).
}
\examples{
set.seed(1)
base <- rnorm(40, 50, 10)
retest <- base + rnorm(40, 0, 3)
estimateMDC(cbind(base, retest))
}
\references{
Shrout & Fleiss (1979); de Vet et al. (2006).
}
\seealso{
\code{\link[=estimateMCID_distribution]{estimateMCID_distribution()}}, \code{\link[=estimateMCID_anchor]{estimateMCID_anchor()}}
}
