% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clinimetrics-estimate.R
\name{estimateMCID_anchor}
\alias{estimateMCID_anchor}
\title{Anchor-based MCID}
\usage{
estimateMCID_anchor(
  change,
  anchor,
  method = c("roc", "mean_change", "predictive"),
  direction = c("increase", "decrease")
)
}
\arguments{
\item{change}{Numeric change scores (follow-up minus baseline).}

\item{anchor}{Improvement indicator aligned to \code{change}: logical, or 0/1,
where TRUE/1 marks an (minimally) improved case.}

\item{method}{\code{"roc"} (default), \code{"mean_change"} or
\code{"predictive"}.}

\item{direction}{\code{"increase"} (default) if a higher change means
improvement, or \code{"decrease"} if a lower change does.}
}
\value{
The MCID estimate (on the change scale).
}
\description{
Estimates the MCID from change scores anchored to an external improvement
indicator. \code{"roc"} returns the change cut-off that maximizes the Youden
index for classifying the anchor; \code{"mean_change"} the mean change among
the (minimally) improved; \code{"predictive"} the change at which a logistic
model of the anchor crosses probability 0.5.
}
\examples{
set.seed(1)
change <- c(rnorm(50, 1, 2), rnorm(50, 8, 2))
anchor <- rep(c(0, 1), each = 50)
estimateMCID_anchor(change, anchor)
}
\references{
Jaeschke et al. (1989); Copay et al. (2007).
}
\seealso{
\code{\link[=estimateMDC]{estimateMDC()}}, \code{\link[=estimateMCID_distribution]{estimateMCID_distribution()}}
}
