% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/com-instruments-pain.R
\name{scoreBPI}
\alias{scoreBPI}
\title{Score the Brief Pain Inventory (short form)}
\usage{
scoreBPI(items, ...)
}
\arguments{
\item{items}{Named numeric responses (e.g. \code{worst_pain = 8},
\code{interference_sleep = 5}) or an unnamed vector in the instrument's item
order.}

\item{...}{Passed to \code{\link[=scoreInstrument]{scoreInstrument()}} (e.g. \code{missing = "prorate"}).}
}
\value{
A \link{ClinicalScore} whose \code{subscales} hold the severity and
interference means.
}
\description{
Scores the BPI severity (worst / least / average / current pain) and
interference (general activity, mood, walking, work, relations, sleep,
enjoyment of life) items, each 0-10. Severity and interference are reported
as their mean subscale scores (each 0-10, the conventional BPI summaries);
the total is the grand mean over all items.
}
\examples{
scoreBPI(stats::setNames(c(8, 2, 5, 4, 5, 6, 5, 7, 3, 5, 6),
                         getInstrument("bpi")@items))
}
\seealso{
\code{\link[=scorePainNRS]{scorePainNRS()}}, \code{\link[=getInstrument]{getInstrument()}}
}
