% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rasch-responder.R
\name{raschResponder}
\alias{raschResponder}
\title{Responder analysis of pre-to-post change on the Rasch interval scale}
\usage{
raschResponder(
  pre,
  post,
  mcid = NULL,
  confidence = 0.95,
  direction = c("increase", "decrease")
)
}
\arguments{
\item{pre, post}{Baseline and follow-up measures: a \code{poly_rasch} fit, a list
with \code{theta}/\code{theta_se}, or a numeric measure vector. \code{se} is required (from
the fit) to derive the MDC.}

\item{mcid}{Optional MCID on the logit scale; enables the four-level
\code{\link[=classifyResponder]{classifyResponder()}} output.}

\item{confidence}{Confidence level for the MDC (default 0.95).}

\item{direction}{\code{"increase"} (default; higher measure = better) or
\code{"decrease"}.}
}
\value{
a \code{data.frame}, one row per person: \code{pre}, \code{post}, \code{change},
\code{improvement} (direction-aware), \code{sem}, \code{mdc}, \code{reliable_change} (from
\code{\link[=mdcResponder]{mdcResponder()}}) and, if \code{mcid} given, \code{classification} (from
\code{\link[=classifyResponder]{classifyResponder()}}). Attributes \code{sem}, \code{mdc}, \code{mcid} record the values used.
}
\description{
Classifies each person's change between two occasions using interval (logit)
measures from \code{\link[=raschAnalyze]{raschAnalyze()}} / \code{\link[PhysioAppKit:pcm_measure]{PhysioAppKit::pcm_measure()}}. The Rasch
model's own standard errors give a distribution-free standard error of
measurement and hence the Minimal Detectable Change; the pre/post change is
then run through \code{\link[=mdcResponder]{mdcResponder()}} (reliable change) and, when an MCID on the
logit scale is supplied, \code{\link[=classifyResponder]{classifyResponder()}} (the MDC x MCID framework).
}
\details{
The two occasions must be on a common metric -- calibrate the items once and
anchor them, or co-calibrate -- and the persons must align by row.
}
\examples{
pre  <- list(theta = c(0, 0, 0, 0), theta_se = rep(0.3, 4))
post <- list(theta = c(1.5, 0.1, -1.2, 0.05), theta_se = rep(0.3, 4))
raschResponder(pre, post, mcid = 1.0)
}
\seealso{
\code{\link[=raschAnalyze]{raschAnalyze()}}, \code{\link[=mdcResponder]{mdcResponder()}}, \code{\link[=classifyResponder]{classifyResponder()}},
\code{\link[=estimateMDC]{estimateMDC()}}
}
